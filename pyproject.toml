[project]
name = "synthetic-dataset-generator"
version = "0.1.0"
description = "LLM-based synthetic student dataset generator using Ollama and Llama 3.2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.11.0",
    "ollama>=0.6.1",
    "pandas>=3.0.2",
]
